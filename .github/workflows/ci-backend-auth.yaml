name: Backend Security CI

on:
  push:
    branches: [ main, develop, E2E ]
  pull_request:
    branches: [ main, develop, E2E ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-24.04

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: dispatchiq
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres123
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4.2.2

    - name: Set up JDK 21
      id: setup-jdk
      uses: actions/setup-java@v4.4.0
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'maven'

    - name: Grant execute permission for Maven Wrapper
      run: chmod +x ./mvnw

    - name: Run Targeted Security Tests
      # CRITICAL FIX: Directly hardcode the JAVA_HOME path to point to the freshly installed JDK 21
      run: |
        export JAVA_HOME=$JAVA_HOME_21_X64
        export PATH=$JAVA_HOME/bin:$PATH
        java -version
        ./mvnw -pl apps/backend clean test -Dtest=SecurityAuthTest -Dspring.profiles.active=test